module.exports.register =(req,resp)=>{
    resp.json({msg:'Register'})
}

module.exports.login =(req,resp)=>{
    resp.json({msg:'Login'})
}

module.exports.getMe =(req,resp)=>{
    resp.json({msg:'Getme...'})
}